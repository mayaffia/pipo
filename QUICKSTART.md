# 🚀 Быстрый старт - Task Management System

## Где вы сейчас находитесь?

Проверьте текущую директорию:

```bash
pwd
```

Вы должны увидеть что-то вроде:

```
/Users/mayaffia/Desktop/pipo_project
```

Это **корневая директория проекта**. Все команды ниже выполняются отсюда!

## 📁 Структура файлов

```
pipo_project/                    ← ВЫ ЗДЕСЬ (корневая директория)
├── backend/                     ← Backend код
│   ├── src/
│   ├── package.json
│   └── .env.example
├── frontend/                    ← Frontend код
│   ├── src/
│   ├── package.json
│   └── .env.example
├── k8s/                         ← Kubernetes манифесты
├── docker-compose.yml           ← Файл для Docker Compose
├── README.md
└── SETUP.md
```

## ⚡ Самый быстрый способ запуска

### Шаг 1: Убедитесь, что вы в корневой директории

```bash
# Проверьте, что вы видите docker-compose.yml
ls -la | grep docker-compose.yml

# Если не видите, перейдите в корневую директорию проекта
cd /Users/mayaffia/Desktop/pipo_project
```

### Шаг 2: Запустите Docker Compose

```bash
# Из корневой директории (где находится docker-compose.yml)
docker-compose up --build
```

**Важно:** Команда `docker-compose up --build` запускается **ТОЛЬКО из корневой директории**, где находится файл `docker-compose.yml`!

### Шаг 3: Откройте приложение

После запуска откройте в браузере:

- **Frontend**: http://localhost:3000
- **Backend API**: http://localhost:8000
- **Swagger документация**: http://localhost:8000/api-docs
- **Метрики**: http://localhost:8000/metrics

## 🔧 Альтернатива: Запуск без Docker

Если Docker не работает, можно запустить локально:

### 1. Запустите PostgreSQL

```bash
# Из корневой директории
docker run -d \
  --name postgres \
  -e POSTGRES_USER=taskuser \
  -e POSTGRES_PASSWORD=taskpass \
  -e POSTGRES_DB=taskdb \
  -p 5432:5432 \
  postgres:15-alpine
```

### 2. Запустите Backend

```bash
# Перейдите в директорию backend
cd backend

# Установите зависимости
npm install

# Создайте .env файл
cp .env.example .env

# Запустите в режиме разработки
npm run dev
```

Backend будет доступен на http://localhost:8000

### 3. Запустите Frontend (в новом терминале)

```bash
# Из корневой директории перейдите в frontend
cd frontend

# Установите зависимости
npm install

# Создайте .env файл
cp .env.example .env

# Запустите в режиме разработки
npm run dev
```

Frontend будет доступен на http://localhost:3000

## ❓ Частые проблемы

### "docker-compose: command not found"

Установите Docker Desktop:

- macOS: https://docs.docker.com/desktop/install/mac-install/
- Windows: https://docs.docker.com/desktop/install/windows-install/

### "No such file or directory: docker-compose.yml"

Вы не в корневой директории! Выполните:

```bash
cd /Users/mayaffia/Desktop/pipo_project
ls -la docker-compose.yml  # Проверьте, что файл есть
```

### "Port 5432 already in use"

PostgreSQL уже запущен. Остановите его:

```bash
docker stop postgres
docker rm postgres
```

### "cp: .env.example: No such file or directory"

Вы в неправильной директории. Проверьте:

```bash
pwd  # Покажет текущую директорию

# Если вы в backend/, просто выполните:
cp .env.example .env

# Если вы в корневой директории:
cp backend/.env.example backend/.env
```

## 📝 Краткая шпаргалка команд

```bash
# Запуск всего проекта (из корневой директории)
docker-compose up --build

# Остановка
docker-compose down

# Просмотр логов
docker-compose logs -f

# Перезапуск только backend
docker-compose restart backend

# Перезапуск только frontend
docker-compose restart frontend
```

## ✅ Проверка работы

После запуска проверьте:

1. **Backend работает:**

```bash
curl http://localhost:8000/health
# Должно вернуть: {"status":"ok","timestamp":"..."}
```

2. **Frontend открывается:**
   Откройте http://localhost:3000 в браузере

3. **База данных работает:**

```bash
docker ps | grep postgres
# Должен показать запущенный контейнер postgres
```

## 🎯 Следующие шаги

1. Зарегистрируйте пользователя на http://localhost:3000/register
2. Войдите в систему
3. Создайте первую задачу
4. Посмотрите Swagger документацию на http://localhost:8000/api-docs

Готово! 🎉
